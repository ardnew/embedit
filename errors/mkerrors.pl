use strict;
use warnings;

my @x = qw[
  InvalidReceiver 
  InvalidArgument
];

# Construct our generating command line.
print qx[  ps --no-headers -o args $$ ];

my @decl = (
  "type (",
)

my @ = (
  "type (",
)
